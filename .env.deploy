# SSH Configuration for VPS
SSH_USER=your_ssh_user
SSH_HOST=your_server_ip
SSH_PORT=22

# Docker Image Configuration
IMAGE_NAME=personal-evolution-api-image
DOCKERFILE_DIR=Dockerfile

# Optional: Remote directory (defaults to /home/$SSH_USER/docker-images)
# REMOTE_IMAGE_DIR=/path/to/remote/images
